services:
#    scrum.example:
#        class: ScrumBundle\Example
#        arguments: ["@service_id", "plain_value", "%parameter%"]
nelmio_cors:
  defaults:
    allow_origin: ["*"]
    allow_headers: ["*"]
    allow_methods: ["POST", "PUT", "GET", "DELETE", "OPTIONS"]
    max_age: 3600
    origin_regex: false
  paths:
    '^/': ~